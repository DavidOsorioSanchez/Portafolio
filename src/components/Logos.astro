---
interface Props {
    miAnimacion: string;
    links: string;
}

const { miAnimacion, links } = Astro.props;
---

<img class={`iconoProgramacion ${miAnimacion}`} src={links} alt="no cargo T_T" loading="eager" />